package 프로그래머스_MYSQL;

public class LV1_어린동물찾기 {
    /**
     * 이 테이블에서 어린 동물에 id와 이름을 조회하고, id순으로 보여주세요
     * SELECT ANIMAL_ID, NAME
     * FROM ANIMAL_INS
     * WHERE INTAKE_CONDITION != 'AGED'
     * ORDER BY ANIMAL_ID ASC
     */
}
