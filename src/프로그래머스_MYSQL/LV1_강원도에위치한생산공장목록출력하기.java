package 프로그래머스_MYSQL;

public class LV1_강원도에위치한생산공장목록출력하기 {
    /**
     * 강원도에 위치한 id, 이름, 주소를 id 오름차순으로 조회하는 쿼리 작성하기
     * SELECT FACTORY_ID, FACTORY_NAME, ADDRESS
     * FROM FOOD_FACTORY
     * WHERE ADDRESS LIKE '%강원도%'
     * ORDER BY FACTORY_ID
     */
}
