package 프로그래머스_MYSQL;

public class LV2_중복제거하기 {
    /**
     * 동물의 이름이 몇개인지 조회하는 sql문, null은 집계하지 않고, 종복되는 이름은 하나이다.
     *
     * SELECT COUNT(DISTINCT NAME)
     * FROM ANIMAL_INS
     * WHERE NAME IS NOT NULL
     */
}
